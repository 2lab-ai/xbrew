name = "telegram"
description = "Telegram Desktop"

# Arch: the official repo package (its name differs from the friendly "telegram").
[arch]
pacman = "telegram-desktop"

# Debian/Ubuntu: apt package (in universe).
[debian]
apt = "telegram-desktop"

# macOS: Homebrew cask.
[macos]
cask = "telegram"
